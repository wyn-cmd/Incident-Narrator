# Incident-Narrator
A simple personal python project that parses through given PCAP files to to generate a human-readable report of the packets.


# Installation

Simply run:
`pip install scapy pandas`
Then run the `main.py` file to start analysing the 'capture.pcap' file in the same directory.
